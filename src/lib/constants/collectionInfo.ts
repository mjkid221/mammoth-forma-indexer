import { env } from "~/env";

export const collectionMaxSupply = env.NEXT_PUBLIC_COLLECTION_MAX_SUPPLY;
export const projectName = env.NEXT_PUBLIC_PROJECT_NAME;
export const nativeCurrency = env.NEXT_PUBLIC_NATIVE_CURRENCY;
