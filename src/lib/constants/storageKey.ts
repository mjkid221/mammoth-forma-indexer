// Keys for local storage
export const generateChartTypeKey = (title: string) => `chart-type-${title}`;
export const generateFilterKey = (title: string) => `filter-${title}`;
export const generateTimeFrameKey = (title: string) => `time-frame-${title}`;
